<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AETHER | Transcend Your Limits</title>
    <!-- 
      AETHER - High-End Digital Experience
      This file is optimized for GitHub Pages.
    -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <script src="https://cdn.jsdelivr.net/gh/studio-freight/lenis@1.0.19/bundled/lenis.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Syncopate:wght@400;700&family=Noto+Sans+JP:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #050505;
            --accent: #DEFF9A;
            --text: #ffffff;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text);
            font-family: 'Noto Sans JP', sans-serif;
            overflow-x: hidden;
            line-height: 1.6;
        }

        .font-syncopate { font-family: 'Syncopate', sans-serif; }

        /* Smooth Scroll Setup */
        .lenis.lenis-smooth { scroll-behavior: auto !important; }

        /* Custom Cursor */
        #cursor {
            position: fixed;
            width: 12px;
            height: 12px;
            background: var(--accent);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9999;
            mix-blend-mode: difference;
            transform: translate(-50%, -50%);
        }

        /* Aesthetics */
        nav.scrolled {
            background: rgba(0,0,0,0.85);
            backdrop-filter: blur(15px);
            padding-top: 1rem;
            padding-bottom: 1rem;
        }

        .reveal-box {
            position: relative;
            overflow: hidden;
            clip-path: inset(0 0 100% 0);
        }

        .noise {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: url('https://grainy-gradients.vercel.app/noise.svg');
            opacity: 0.04;
            pointer-events: none;
            z-index: 9000;
        }

        .parallax-text {
            font-size: 20vw;
            color: rgba(255, 255, 255, 0.02);
            position: absolute;
            z-index: -1;
            font-weight: 700;
            text-transform: uppercase;
        }

        .btn-primary {
            background: var(--accent);
            color: #000;
            font-weight: 700;
            padding: 1rem 2.5rem;
            border-radius: 9999px;
            text-transform: uppercase;
            letter-spacing: 0.2em;
            font-size: 0.7rem;
            transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(222, 255, 154, 0.3);
        }

        /* Transitions */
        .page-section {
            transition: opacity 0.5s ease;
        }
        
        ::-webkit-scrollbar { display: none; }
    </style>
</head>
<body>
    <div id="cursor"></div>
    <div class="noise"></div>

    <!-- Navigation -->
    <nav id="navbar" class="fixed top-0 w-full z-50 p-8 flex justify-between items-center transition-all duration-500">
        <div class="font-syncopate text-2xl font-bold tracking-tighter cursor-pointer" onclick="showPage('home')">AETHER</div>
        
        <div class="hidden md:flex gap-10 font-syncopate text-[9px] tracking-[0.3em] uppercase">
            <a href="javascript:void(0)" onclick="showPage('programs')" class="hover:text-[#DEFF9A] transition-colors">Programs</a>
            <a href="javascript:void(0)" onclick="showPage('about')" class="hover:text-[#DEFF9A] transition-colors">Philosophy</a>
            <a href="javascript:void(0)" onclick="showPage('membership')" class="hover:text-[#DEFF9A] transition-colors">Membership</a>
        </div>

        <div class="flex items-center gap-4">
            <button id="loginBtn" onclick="toggleModal(true)" class="border border-white/20 px-6 py-2 rounded-full text-[9px] uppercase tracking-widest hover:bg-white hover:text-black transition-all">
                Login
            </button>
            <button id="portalBtn" onclick="showPage('portal')" class="hidden bg-[#DEFF9A] text-black px-6 py-2 rounded-full text-[9px] font-bold uppercase tracking-widest hover:scale-105 transition-all">
                Portal
            </button>
        </div>
    </nav>

    <!-- CONTENT WRAPPER -->
    <div id="content">
        <!-- HOME PAGE -->
        <section id="home" class="page-section">
            <div class="h-screen flex flex-col justify-center items-center text-center px-4 relative">
                <div class="parallax-text font-syncopate" id="hero-bg">AETHER</div>
                <h1 class="font-syncopate text-6xl md:text-[10vw] font-bold leading-none tracking-tighter mb-6" id="hero-title">
                    SCULPT YOUR<br><span class="opacity-30">EXISTENCE</span>
                </h1>
                <p class="text-[10px] tracking-[0.6em] opacity-40 uppercase">Beyond the physical, into the essence.</p>
                <div class="absolute bottom-10 left-1/2 -translate-x-1/2 opacity-20 text-[9px] tracking-widest uppercase">Scroll to Explore</div>
            </div>

            <!-- Intro Section -->
            <div class="px-6 md:px-24 py-32 flex flex-col md:flex-row gap-20 items-center">
                <div class="w-full md:w-1/2 overflow-hidden rounded-2xl reveal-box" id="intro-img">
                    <img src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?q=80&w=2070&auto=format&fit=crop" class="w-full grayscale brightness-75 transition-transform duration-[2s] hover:scale-110" alt="Gym Interior">
                </div>
                <div class="w-full md:w-1/2 space-y-8">
                    <span class="text-[#DEFF9A] font-syncopate text-xs tracking-[0.3em]">01 / THE CONCEPT</span>
                    <h2 class="text-4xl md:text-7xl font-bold leading-tight tracking-tighter">科学と美学の、<br>究極の融合。</h2>
                    <p class="text-gray-500 max-w-md leading-relaxed text-sm">
                        AETHERは、単なるトレーニングジムではありません。DNA解析、バイオメトリクス、そして空間設計。すべてはあなたが「自己を超越」するための装置として機能します。
                    </p>
                    <button onclick="showPage('about')" class="btn-primary">View Philosophy</button>
                </div>
            </div>
        </section>

        <!-- PROGRAMS PAGE -->
        <section id="programs" class="page-section hidden pt-40 px-6 md:px-24 min-h-screen">
            <span class="text-[#DEFF9A] font-syncopate text-xs tracking-[0.3em] block mb-4">02 / SERVICES</span>
            <h2 class="font-syncopate text-6xl md:text-9xl font-bold italic opacity-10 leading-none">PROGRAMS</h2>
            <div class="grid md:grid-cols-2 gap-12 mt-[-2rem] relative z-10">
                <div class="bg-white/5 backdrop-blur-md p-12 rounded-3xl border border-white/10 hover:border-[#DEFF9A]/50 transition-colors">
                    <h3 class="text-3xl font-bold mb-6 text-[#DEFF9A]">DNA-Based Training</h3>
                    <p class="text-gray-400 leading-loose">個々の遺伝的特性を分析し、最適な運動強度と栄養摂取を科学的に導き出します。無駄を削ぎ落とし、最短で最高の結果へ。</p>
                </div>
                <div class="bg-white/5 backdrop-blur-md p-12 rounded-3xl border border-white/10 hover:border-[#DEFF9A]/50 transition-colors">
                    <h3 class="text-3xl font-bold mb-6 text-[#DEFF9A]">Bio-Feedback System</h3>
                    <p class="text-gray-400 leading-loose">ウェアラブルデバイスにより24時間のバイオリズムを監視し、セッション内容をリアルタイムで調整。常に「今」のあなたに最適化されます。</p>
                </div>
            </div>
        </section>

        <!-- ABOUT PAGE -->
        <section id="about" class="page-section hidden pt-40 px-6 md:px-24 min-h-screen text-center">
            <h2 class="font-syncopate text-6xl md:text-8xl font-bold mb-12">PHILOSOPHY</h2>
            <p class="max-w-3xl mx-auto text-xl md:text-2xl font-light leading-relaxed text-gray-400">
                「静寂の中で、肉体は覚醒する。」<br><br>
                私たちは、トレーニングを単なる苦行とは考えません。それは自分自身と対話し、余分なものを削ぎ落とし、本来の輝きを取り出す「彫刻」のようなプロセスです。AETHERは、そのための聖域を提供します。
            </p>
        </section>

        <!-- MEMBERSHIP PAGE -->
        <section id="membership" class="page-section hidden pt-40 px-6 md:px-24 min-h-screen">
            <h2 class="font-syncopate text-6xl md:text-8xl font-bold mb-20 text-center italic">TIERS</h2>
            <div class="grid md:grid-cols-2 gap-10 max-w-6xl mx-auto">
                <div class="p-16 rounded-[3rem] border border-white/10 bg-white/5 flex flex-col justify-between">
                    <div>
                        <h3 class="font-syncopate text-xs tracking-[0.4em] mb-10 opacity-40 uppercase">Core Membership</h3>
                        <div class="text-6xl font-bold mb-10 tracking-tighter">¥44,000<span class="text-xs font-normal opacity-30 italic ml-2">/month</span></div>
                        <ul class="space-y-4 text-sm text-gray-500 mb-20">
                            <li>• 月4回のパーソナルセッション</li>
                            <li>• DNA解析 & カウンセリング</li>
                            <li>• ウェア & アメニティ完全貸出</li>
                        </ul>
                    </div>
                    <button class="w-full border border-white/20 py-5 rounded-full uppercase tracking-widest text-[10px] hover:bg-white hover:text-black transition-all">Select Plan</button>
                </div>
                <div class="p-16 rounded-[3rem] border border-[#DEFF9A]/30 bg-white/5 relative overflow-hidden flex flex-col justify-between">
                    <div class="absolute top-8 right-8 bg-[#DEFF9A] text-black text-[8px] font-bold px-3 py-1 rounded-full uppercase tracking-tighter">Most Exclusive</div>
                    <div>
                        <h3 class="font-syncopate text-xs tracking-[0.4em] mb-10 text-[#DEFF9A] uppercase">Prestige Tier</h3>
                        <div class="text-6xl font-bold mb-10 tracking-tighter">¥110,000<span class="text-xs font-normal opacity-30 italic ml-2">/month</span></div>
                        <ul class="space-y-4 text-sm text-gray-500 mb-20">
                            <li>• パーソナルセッション無制限</li>
                            <li>• 専属トレーナー24時間チャット</li>
                            <li>• 高度血液分析 & サプリ提供</li>
                        </ul>
                    </div>
                    <button class="w-full btn-primary">Join Prestige</button>
                </div>
            </div>
        </section>

        <!-- PORTAL PAGE (LoggedIn) -->
        <section id="portal" class="page-section hidden pt-40 px-6 md:px-24 min-h-screen">
            <div class="flex flex-col md:flex-row justify-between items-end border-b border-white/10 pb-12 mb-12 gap-6">
                <div>
                    <h2 class="text-4xl md:text-6xl font-bold font-syncopate tracking-tighter">MEMBER PORTAL</h2>
                    <p class="text-[#DEFF9A] text-[10px] tracking-[0.4em] mt-4 uppercase font-bold">Welcome back, Mr. Takahashi</p>
                </div>
                <button onclick="logout()" class="text-[9px] tracking-[0.4em] opacity-30 hover:opacity-100 transition-opacity uppercase font-bold">Terminate Session</button>
            </div>
            
            <div class="grid md:grid-cols-3 gap-8">
                <div class="col-span-2 bg-white/5 p-12 rounded-[2.5rem] border border-white/5">
                    <h3 class="text-[10px] tracking-[0.3em] text-gray-500 mb-10 uppercase font-bold">Next Session</h3>
                    <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-10">
                        <div>
                            <div class="text-4xl md:text-5xl font-bold mb-2">06.15 <span class="text-xl opacity-30 italic">SAT</span></div>
                            <div class="text-2xl font-bold text-gray-400">10:00 - 11:30</div>
                            <div class="text-[#DEFF9A] text-xs mt-4 tracking-widest uppercase">Trainer: Kenji. S</div>
                        </div>
                        <button class="bg-white text-black px-10 py-4 rounded-full text-[10px] font-bold uppercase tracking-widest hover:scale-105 transition-all">Reschedule</button>
                    </div>
                </div>
                <div class="bg-white/5 p-12 rounded-[2.5rem] border border-white/5 flex flex-col justify-between">
                    <h3 class="text-[10px] tracking-[0.3em] text-gray-500 mb-10 uppercase font-bold">Metrics</h3>
                    <div class="space-y-8">
                        <div>
                            <div class="flex justify-between items-end mb-3"><span class="text-[10px] uppercase opacity-40">Muscle Mass</span><span class="text-2xl font-bold">38.2kg</span></div>
                            <div class="w-full bg-white/5 h-[1px]"><div class="bg-[#DEFF9A] h-full w-[85%] shadow-[0_0_10px_#DEFF9A]"></div></div>
                        </div>
                        <div>
                            <div class="flex justify-between items-end mb-3"><span class="text-[10px] uppercase opacity-40">Body Fat</span><span class="text-2xl font-bold">12.8%</span></div>
                            <div class="w-full bg-white/5 h-[1px]"><div class="bg-white/20 h-full w-[40%]"></div></div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>

    <!-- Login Modal -->
    <div id="loginModal" class="fixed inset-0 z-[100] hidden items-center justify-center p-6 bg-black/95 backdrop-blur-2xl">
        <div class="bg-white/5 border border-white/10 w-full max-w-md p-12 rounded-[3rem] relative">
            <button onclick="toggleModal(false)" class="absolute top-10 right-10 opacity-30 hover:opacity-100 transition-opacity">✕</button>
            <h3 class="font-syncopate text-3xl font-bold mb-10 italic tracking-tighter">ACCESS PORTAL</h3>
            <form onsubmit="login(event)" class="space-y-8">
                <div>
                    <label class="text-[8px] tracking-[0.4em] uppercase opacity-30 block mb-3 font-bold">Identification</label>
                    <input type="text" class="w-full bg-white/5 border border-white/10 rounded-2xl p-5 text-sm focus:border-[#DEFF9A] outline-none transition-colors" placeholder="ID or Email">
                </div>
                <div>
                    <label class="text-[8px] tracking-[0.4em] uppercase opacity-30 block mb-3 font-bold">Security Key</label>
                    <input type="password" class="w-full bg-white/5 border border-white/10 rounded-2xl p-5 text-sm focus:border-[#DEFF9A] outline-none transition-colors" value="password">
                </div>
                <button type="submit" class="w-full btn-primary mt-6">Authorize</button>
            </form>
        </div>
    </div>

    <footer class="mt-60 p-20 border-t border-white/5 text-center">
        <div class="font-syncopate text-2xl mb-6 tracking-tighter">AETHER</div>
        <p class="text-[8px] tracking-[0.6em] opacity-20 uppercase font-bold">©2024 AETHER DIGITAL STRATEGY LAB / TOKYO</p>
    </footer>

    <script>
        // --- Smooth Scroll (Lenis) ---
        const lenis = new Lenis();
        function raf(time) {
            lenis.raf(time);
            requestAnimationFrame(raf);
        }
        requestAnimationFrame(raf);

        // --- Animations (GSAP) ---
        gsap.registerPlugin(ScrollTrigger);

        // Cursor Movement
        const cursor = document.getElementById('cursor');
        window.addEventListener('mousemove', e => {
            gsap.to(cursor, { x: e.clientX, y: e.clientY, duration: 0.2 });
        });

        // Hero Parallax
        gsap.to("#hero-bg", {
            scrollTrigger: { scrub: true },
            x: 250,
            opacity: 0.1
        });

        gsap.to("#hero-title", {
            scrollTrigger: { scrub: true },
            y: -150,
            scale: 0.9,
            opacity: 0
        });

        // Image Reveal
        gsap.to(".reveal-box", {
            scrollTrigger: {
                trigger: ".reveal-box",
                start: "top 85%",
                toggleActions: "play none none none"
            },
            clipPath: "inset(0 0 0% 0)",
            duration: 2,
            ease: "expo.out"
        });

        // Navbar Interaction
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 100) nav.classList.add('scrolled');
            else nav.classList.remove('scrolled');
        });

        // --- SPA Engine ---
        function showPage(pageId) {
            const sections = document.querySelectorAll('.page-section');
            sections.forEach(s => {
                s.style.opacity = '0';
                setTimeout(() => s.classList.add('hidden'), 500);
            });
            
            setTimeout(() => {
                const target = document.getElementById(pageId);
                target.classList.remove('hidden');
                setTimeout(() => target.style.opacity = '1', 50);
                window.scrollTo(0, 0);
            }, 500);
        }

        // --- Auth Logic ---
        function toggleModal(show) {
            const modal = document.getElementById('loginModal');
            modal.style.display = show ? 'flex' : 'none';
            if(show) document.body.style.overflow = 'hidden';
            else document.body.style.overflow = 'auto';
        }

        function login(e) {
            e.preventDefault();
            toggleModal(false);
            document.getElementById('loginBtn').classList.add('hidden');
            document.getElementById('portalBtn').classList.remove('hidden');
            showPage('portal');
        }

        function logout() {
            document.getElementById('loginBtn').classList.remove('hidden');
            document.getElementById('portalBtn').classList.add('hidden');
            showPage('home');
        }
    </script>
</body>
</html>

